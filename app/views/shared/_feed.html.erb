<% if @feed_items.any? %>
    <ol class="microposts">
        <% #know to render _micropost.html.erb, because of @feed_items.class %>
        <%= render @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>
<% end %>